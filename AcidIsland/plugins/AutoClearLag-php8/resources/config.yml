---
# AutoClearLagg (ACL) Config

# Seconds between each clear lagg
seconds: 4800

# Entities to clear
clear:
  items: true
  mobs: true
  xp-orbs: true
  exempt: # Array of entities not to clear
    - Zombie
    - Pig

messages:
  time-left: "§cServer dọn rác sau {SECONDS} giây"
  entities-cleared: "§cĐã dọn {COUNT} rác"

# Countdown times
times: [60, 30, 15, 10, 5, 4, 3, 2, 1]
...
